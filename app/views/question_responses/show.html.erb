<%= render "body" do %>
  <div class="text-9xl">
    <% if @response.correct? %>
     That's Right!
   <% else %>
     Wrong!
    <% end %>
  </div>

  <%= link_to "Play Again", root_path,
    data: { controller: "keyboard-shortcut", action: "keypress@document->keyboard-shortcut#fire", keyboard_shortcut_key_value: "Enter" },
    class: "button" %>
<% end %>
